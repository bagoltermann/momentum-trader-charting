# Session Notes - 2026-03-09

## Status
Trader app integration audit (v1.93.2 → v1.96.0.1), streaming seed fix for zero-candle symbols, streaming health monitor, and quality score sidebar display.

## Session Overview
1. **Trader app audit** — Reviewed 20 commits for charting app impact. No breaking changes found.
2. **OTC "No trades yet" investigation** — Diagnosed why some watchlist symbols show zero candles. Root cause: Schwab provides no intraday data for OTC foreign stocks. Trader app already fixed at scanner level (v1.84.4 OTC filter).
3. **Streaming seed fix** — When REST returns all-zero candles, `updateStreamCandle` now seeds the array from streaming ticks instead of discarding them. Committed as `2807759`.
4. **Streaming Health Monitor** — New StatusBar indicator showing LIVE/DELAYED/STALE feed status from trader app's v1.94.0 watchdog endpoint.
5. **Quality Score in Sidebar** — Display `Q:N` badge per symbol, color-coded (green >=7, yellow 4-6, gray <4). Data was already flowing but not rendered.

## Work Completed

### Trader App Integration Audit (v1.93.2 → v1.96.0.1)

Key findings from 20 commits:
- **v1.95.0**: Delta-aware streaming (Schwab sends partial updates). Charting app already handles this correctly.
- **v1.94.0**: Streaming data flow watchdog with new `/api/streaming/health` endpoint. Detects silent WebSocket death.
- **v1.95.0.3**: `quality_score` fixed (was always 0). `wall_classification` and `momentum_phase` now populated.
- **v1.84.4**: OTC foreign symbol filter (4+ chars ending in F/Y) blocks non-streamable symbols at scan boundary. SYAXF incident drove this change.
- **No breaking changes** — all API responses backward-compatible.

### Streaming Seed Fix

**File:** `src/renderer/store/candleDataStore.ts`
**Commit:** `2807759`

When Schwab returns all-zero placeholder candles and streaming quotes eventually arrive, `updateStreamCandle` now seeds the candle array instead of returning early on `primaryCandles.length === 0`. Clears "No trades yet" error.

### Streaming Health Monitor

**Backend:** `backend/api/routes.py` — New `GET /api/streaming/health` proxy to trader app's watchdog endpoint (same httpx pattern as rotation proxy).

**Hook:** `src/renderer/hooks/useStreamingHealth.ts` (NEW)
- Feature flag: `STREAMING_HEALTH_ENABLED = true`
- 10s polling, AbortController + mountedRef cleanup
- Derives status from `last_message_seconds_ago`: <5s=live, 5-30s=delayed, >30s=stale
- Returns formatted quote count (42K, 1.2M)

**StatusBar:** `src/renderer/components/layout/StatusBar.tsx`
- Replaced "Data: Streaming/Polling" with health-aware indicator
- `Feed: LIVE (42K)` (green), `Feed: DELAYED 15s` (yellow), `Feed: STALE 45s!` (red)
- Falls back to old behavior when health endpoint unavailable

**App.tsx:** Imported `useStreamingHealth`, passed to `StatusBar`.

### Quality Score in Sidebar

**Sidebar.tsx:** Added `quality_score?: number` to `WatchlistItem`, renders `Q:N` badge after gap %.
**CSS:** `.quality-score` with `.high` (green >=7), `.mid` (yellow 4-6), `.low` (gray <4).

## Testing Checklist

- [x] Build succeeds with zero TypeScript errors
- [ ] StatusBar shows "Feed: LIVE (NK)" with green dot when trader app running (market hours)
- [ ] StatusBar falls back gracefully when trader app stopped
- [ ] Sidebar shows Q:N for each symbol with color coding
- [ ] Feature flag: `STREAMING_HEALTH_ENABLED = false` disables health polling

## Files Modified

| File | Change | Commit |
|------|--------|--------|
| `src/renderer/store/candleDataStore.ts` | Streaming seed fix for zero-candle symbols | `2807759` |
| `backend/api/routes.py` | New `/streaming/health` proxy endpoint | pending |
| `src/renderer/hooks/useStreamingHealth.ts` | **NEW** — 10s health polling hook | pending |
| `src/renderer/components/layout/StatusBar.tsx` | Health-aware feed indicator | pending |
| `src/renderer/components/layout/Sidebar.tsx` | Quality score badge | pending |
| `src/renderer/App.tsx` | Streaming health hook wiring | pending |
| `src/renderer/styles/global.css` | Quality score styling | pending |

## Key Takeaways

1. **Regular trader app audits** are essential — 20 commits had accumulated since our last session with data format improvements we were missing.
2. **OTC foreign stocks** (symbols ending in F/Y) have no Schwab streaming or candle data. Trader app v1.84.4 filters these at scan boundary.
3. **Silent WebSocket death** is a real production issue (UVIX incident). The health monitor gives visibility into this from the charting app.
4. **quality_score was always 0** before v1.95.0.3 — a key data quality fix we benefit from automatically.

## Next Steps

- [ ] Verify streaming health indicator during market hours
- [ ] Verify quality score values are meaningful (not all same value)
- [ ] Update FEATURE_SUGGESTIONS.md audit log with v1.84-v1.96 findings
